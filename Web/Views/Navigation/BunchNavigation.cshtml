@model Web.Models.NavigationModels.BunchNavigationModel
@if (!Model.IsEmpty)
{
    <nav class="game-nav">
        <h2><a href="@Model.HeadingUrl">@Model.Heading</a></h2>
        <ul>
            <li>
                <a href="@Model.CashgameUrl"><span>Cashgames</span></a>
            </li>
            <li>
                <a href="@Model.PlayerUrl"><span>Players</span></a>
            </li>
            <li>
                <a href="@Model.EventUrl"><span>Events</span></a>
            </li>
        </ul>
    </nav>
}
