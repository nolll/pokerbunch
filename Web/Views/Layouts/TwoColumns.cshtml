@model Web.Models.PageBaseModels.PageModel

@{
    Layout = Model.Layout;
}

@if (IsSectionDefined("Aside1")){
    <div class="region aside">
        @RenderSection("Aside1")
    </div>
}

<div class="region width2">
    @RenderBody()
</div>

@if (IsSectionDefined("Aside2")){
    <div class="region aside">
        @RenderSection("Aside2")
    </div>
}