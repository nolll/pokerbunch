@model Web.Models.PlayerModels.Badges.PlayerBadgesModel
<h2 class="h2">Player Achievements</h2>

<ul class="achievements">
    @Html.Partial("PlayerBadge", @Model.PlayedOneGame)
    @Html.Partial("PlayerBadge", @Model.PlayedTenGames)
    @Html.Partial("PlayerBadge", @Model.Played50Games)
    @Html.Partial("PlayerBadge", @Model.Played100Games)
    @Html.Partial("PlayerBadge", @Model.Played200Games)
    @Html.Partial("PlayerBadge", @Model.Played500Games)
</ul>