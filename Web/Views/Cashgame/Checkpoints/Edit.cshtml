@model Web.Models.CashgameModels.Checkpoints.EditCheckpointPageModel

@{
    Layout = "~/Views/Layouts/OneColumn.cshtml";
}

<div class="block gutter">
	<h1>Edit Checkpoint</h1>
</div>
<div class="block gutter">
    
    @Html.Partial("~/Views/Forms/Errors.cshtml")
    
    <form method="post">
        <fieldset>
            <p>
				@Html.LabelFor(model => model.Timestamp, "Timestamp", new { @class = "label" })
                @Html.TextBoxFor(model => model.Timestamp, new { @class="textfield"})
			</p>
            <p>
                @Html.LabelFor(model => model.Stack, @Model.StackLabel, new { @class = "label" })
                @Html.TextBoxFor(model => model.Stack, new { @class="textfield"})
            </p>
            @if (Model.EnableAmountField)
            {
                <p>
				    @Html.LabelFor(model => model.Amount, "Buyin Amount", new { @class = "label" })
                    @Html.TextBoxFor(model => model.Amount, new { @class="textfield"})
			    </p>
            }
        </fieldset>
        <div class="buttons">
            <button type="submit" class="action">Save Changes</button>
            <a href="@Model.DeleteUrl" class="button warning" data-require="delete-confirmation" data-message="Delete Checkpoint?">Delete</a>
			<a href="@Model.CancelUrl" class="button">Cancel</a>
        </div>
    </form>
</div>