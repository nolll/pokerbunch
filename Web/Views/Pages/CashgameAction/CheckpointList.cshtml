@model List<Web.Models.CashgameModels.Action.CheckpointModel>
<ul class="checkpoint-list">
    @foreach (var checkpoint in Model)
    {
        <li class="checkpoint">
            @Html.Partial("CashgameAction/CheckpointItem", checkpoint)
        </li>
    }
</ul>