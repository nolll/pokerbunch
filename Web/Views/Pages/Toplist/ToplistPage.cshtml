@using Web.Models.PageBaseModels
@model Web.Models.CashgameModels.Toplist.CashgameToplistPageModel

@{
    Layout = PageLayout.OneColumn;
}

<div class="block gutter">
    <top-list-table id="vue-top-list-table">
        <script type="application/json">
            @Html.Raw(Model.ToplistJson)
        </script>
    </top-list-table>
    
    @*Google Analytics injects a script before the last script tag. 
    This empty tag is needed to prevent the ga-script to be injected in the component above*@
    <script type="application/json"></script>
</div>