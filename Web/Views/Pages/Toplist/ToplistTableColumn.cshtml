@model Web.Models.CashgameModels.Toplist.TopListTableColumn
@if (Model.SortingEnabled)
{
    <th class="@Model.CssClass"><a href="@Model.Url">@Model.Text</a></th>
}
else
{
    <th><span>@Model.Text</span></th>
}